resources:
  - name: s1
    type: image
    integration: dockerhub
    pointer:
      sourceName: "library/busybox"
      isPull: true
    seed:
      versionName: latest
      
  - name: docker-creds
    type: image
    integration: dockerhub
    pointer:
      sourceName: "library/nginx"
    seed:
      versionName: latest
  
  # - name: slack-notifications
  #   type: notification
  #   integration: slack
  #   pointer:
  #     recipients:
  #       - "#slack-test"
  
